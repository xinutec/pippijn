# Copy to .env and fill in. .env is gitignored. Load with e.g. `set -a; . ./.env`.
DATABASE_URL=mysql://coach:CHANGEME@127.0.0.1:3308/coach
SESSION_SECRET=CHANGEME-long-random-string
NC_BASE_URL=https://nextcloud.example.org
NC_CLIENT_ID=CHANGEME
NC_CLIENT_SECRET=CHANGEME
NC_REDIRECT_URI=http://localhost:8080/auth/callback
BIND_ADDR=0.0.0.0:8080
# DEV ONLY: mints a session without Nextcloud. Never set in production.
DEV_LOGIN_USER=pippijn
