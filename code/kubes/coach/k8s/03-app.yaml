# GENERATED from dhall/apps/coach.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: "coach-app"
  namespace: coach
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "coach-app"
  template:
    metadata:
      labels:
        app: "coach-app"
    spec:
      containers:
      - env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              key: DATABASE_URL
              name: "coach-secret"
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              key: SESSION_SECRET
              name: "coach-secret"
        - name: NC_BASE_URL
          value: "https://dash.xinutec.org"
        - name: NC_CLIENT_ID
          valueFrom:
            secretKeyRef:
              key: NC_CLIENT_ID
              name: "coach-secret"
        - name: NC_CLIENT_SECRET
          valueFrom:
            secretKeyRef:
              key: NC_CLIENT_SECRET
              name: "coach-secret"
        - name: NC_REDIRECT_URI
          value: "https://coach.xinutec.org/auth/callback"
        - name: RUST_LOG
          value: "info,coach=debug"
        - name: HEALTH_INTERNAL_URL
          value: "http://health-auth.health.svc.cluster.local"
        - name: HEALTH_SERVICE_TOKEN
          valueFrom:
            secretKeyRef:
              key: HEALTH_SERVICE_TOKEN
              name: "coach-secret"
              optional: true
        image: "xinutec/coach:latest"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        name: "coach-app"
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: '1'
            memory: 256Mi
          requests:
            cpu: 50m
            memory: 64Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
          readOnlyRootFilesystem: true
      securityContext:
        runAsGroup: 65532
        runAsNonRoot: true
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: "coach-app"
  namespace: coach
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: "coach-app"
