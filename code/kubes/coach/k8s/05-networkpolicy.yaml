# GENERATED from dhall/apps/coach.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "coach-db-from-app-only"
  namespace: coach
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: "coach-app"
    ports:
    - port: 3306
  podSelector:
    matchLabels:
      app: "coach-db"
  policyTypes:
  - Ingress
