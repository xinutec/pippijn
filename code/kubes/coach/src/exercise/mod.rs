//! Exercise catalog: a global library seeded from the kit (migration 0002),
//! plus user-added custom movements. Movement `pattern` is also the recovery
//! grouping used by the pacing engine.

pub mod repo;
pub mod types;
