//! Periodized program: a mesocycle with explicit per-week volume targets and
//! optional day pins (the hybrid weekly-pool + pin-to-a-day structure). The
//! built-in starter block is generated in code — see `starter`.

pub mod repo;
pub mod starter;
pub mod types;
