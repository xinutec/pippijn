# GENERATED from dhall/apps/home.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: home
  namespace: home
spec:
  replicas: 1
  selector:
    matchLabels:
      app: home
  template:
    metadata:
      labels:
        app: home
    spec:
      containers:
      - command:
        - node
        - dist/server.js
        env:
        - name: PORT
          value: '3000'
        - name: DB_HOST
          value: "home-db"
        - name: DB_NAME
          value: home
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              key: DB_USER
              name: "home-secret"
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              key: DB_PASSWORD
              name: "home-secret"
        - name: INGEST_TOKEN
          valueFrom:
            secretKeyRef:
              key: INGEST_TOKEN
              name: "home-secret"
        image: "xinutec/home:latest"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 15
          periodSeconds: 20
        name: home
        ports:
        - containerPort: 3000
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 200m
            memory: 256Mi
          requests:
            cpu: 10m
            memory: 128Mi
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
      securityContext:
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
---
apiVersion: v1
kind: Service
metadata:
  name: home
  namespace: home
spec:
  ports:
  - port: 80
    targetPort: 3000
  selector:
    app: home
