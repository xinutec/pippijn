# GENERATED from dhall/apps/home.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "home-db-from-app-only"
  namespace: home
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: home
    ports:
    - port: 3306
      protocol: TCP
  podSelector:
    matchLabels:
      app: "home-db"
  policyTypes:
  - Ingress

