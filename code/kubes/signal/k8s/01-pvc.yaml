# GENERATED from dhall/apps/signal.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "signal-db-pvc"
  namespace: signal
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "signal-cli-pvc"
  namespace: signal
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "signal-attachments-pvc"
  namespace: signal
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "signal-irclogs-pvc"
  namespace: signal
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

---
# dev-lint: allow-backup-coverage linked-device keys; re-link from the phone instead; re-downloadable from Signal while the messages remain; a staging copy; amun holds the originals; re-downloadable from Telegram while the messages remain
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "signal-telegram-media-pvc"
  namespace: signal
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
