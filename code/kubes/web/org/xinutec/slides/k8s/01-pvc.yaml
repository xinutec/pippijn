# GENERATED from dhall/sites/slides.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
# dev-lint: allow-backup-coverage derived content — re-pushed from ~/Code/slides via deploy.sh, so a lost PVC is recovered by one re-deploy, not a restore.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "slides-storage"
  namespace: web
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
