# GENERATED from dhall/sites/sinterklaas.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    "cert-manager.io/cluster-issuer": "letsencrypt-prod"
  name: "sinterklaas-ingress"
  namespace: web
spec:
  ingressClassName: nginx
  rules:
  - host: sinterklaas.xinutec.org
    http:
      paths:
      - backend:
          service:
            name: "httpd-sinterklaas"
            port:
              number: 80
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - sinterklaas.xinutec.org
    secretName: "sinterklaas-tls"
