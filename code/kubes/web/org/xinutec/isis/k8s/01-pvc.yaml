# GENERATED from dhall/sites/isis.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
# dev-lint: allow-backup-coverage published copies — the originals live in ~/Code/dicom-scan and are re-pushed, so a lost PVC costs a re-deploy, not a restore.
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "httpd-isis-storage"
  namespace: web
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
