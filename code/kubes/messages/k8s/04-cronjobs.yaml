# GENERATED from dhall/apps/messages.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: "messages-link-fetch"
  namespace: signal
spec:
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      activeDeadlineSeconds: 300
      template:
        metadata:
          labels:
            app: "messages-link-fetch"
        spec:
          containers:
          - command:
            - "link-fetch"
            env:
            - name: DB_HOST
              value: "signal-db"
            - name: DB_NAME
              value: signal
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  key: DB_USER
                  name: "signal-secret"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  key: DB_PASSWORD
                  name: "signal-secret"
            - name: LINK_IMAGES_DIR
              value: "/link-images"
            - name: LINK_FETCH_BATCH
              value: '10'
            - name: ALLOWED_USERS
              value: pippijn
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  key: SESSION_SECRET
                  name: "messages-secret"
            - name: NC_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  key: NC_CLIENT_ID
                  name: "messages-secret"
            - name: NC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  key: NC_CLIENT_SECRET
                  name: "messages-secret"
            - name: NC_BASE_URL
              value: "https://dash.xinutec.org"
            - name: NC_REDIRECT_URI
              value: "https://messages.xinutec.org/auth/callback"
            image: "xinutec/messages:latest"
            name: "messages-link-fetch"
            resources:
              limits:
                memory: 192Mi
              requests:
                cpu: 25m
                memory: 64Mi
            securityContext:
              allowPrivilegeEscalation: false
              capabilities:
                drop:
                - ALL
              readOnlyRootFilesystem: true
            volumeMounts:
            - mountPath: "/link-images"
              name: "link-images"
          restartPolicy: OnFailure
          securityContext:
            runAsGroup: 65532
            runAsNonRoot: true
            runAsUser: 65532
            seccompProfile:
              type: RuntimeDefault
          volumes:
          - name: "link-images"
            persistentVolumeClaim:
              claimName: "messages-link-images-pvc"
  schedule: "*/2 * * * *"
  successfulJobsHistoryLimit: 3
