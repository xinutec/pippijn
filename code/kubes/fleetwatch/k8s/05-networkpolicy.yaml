# GENERATED from dhall/apps/fleetwatch.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "fleetwatch-db-from-app-only"
  namespace: fleetwatch
spec:
  ingress:
  - from:
    - podSelector:
        matchLabels:
          app: "fleetwatch-app"
    ports:
    - port: 3306
  podSelector:
    matchLabels:
      app: "fleetwatch-db"
  policyTypes:
  - Ingress
