//! The to-do list — typed tasks (purchase/call to start) with open/done status,
//! offline-first like shopping. Typed, directional connections to other to-dos
//! and app entities are a separate concern (`todo_link`, added next).

pub mod links;
pub mod repo;
pub mod types;
