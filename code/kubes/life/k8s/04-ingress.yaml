# GENERATED from dhall/apps/life.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "life-ingress"
  namespace: life
spec:
  ingressClassName: nginx
  rules:
  - host: life.xinutec.org
    http:
      paths:
      - backend:
          service:
            name: "life-app"
            port:
              number: 80
        path: /
        pathType: Prefix
