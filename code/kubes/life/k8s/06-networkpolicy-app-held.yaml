# GENERATED from dhall/apps/life.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
#
# NOT YET APPLIED. k3s enforces NetworkPolicy via kube-router, which does NOT
# exempt node-sourced kubelet health-probe traffic, so this policy as written
# drops the liveness/readiness probes, marks the pod NotReady, and takes the
# site down. Before applying: admit the probe source as well (an ipBlock for
# the node/pod CIDR), then verify probes stay green on a live pod.
#
# Intent: the app is reachable only through the nginx ingress controller, so
# no other pod can hit the API directly and bypass its TLS termination. The
# selector uses the namespace automatic kubernetes.io/metadata.name label
# rather than chart pod labels, which can change across chart versions.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "life-app-from-ingress-only"
  namespace: life
spec:
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: "ingress-nginx"
    ports:
    - port: 8080
  podSelector:
    matchLabels:
      app: "life-app"
  policyTypes:
  - Ingress
