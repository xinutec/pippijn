# GENERATED from dhall/apps/health.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "health-ingress"
  namespace: health
spec:
  ingressClassName: nginx
  rules:
  - host: health.xinutec.org
    http:
      paths:
      - backend:
          service:
            name: "health-auth"
            port:
              number: 80
        path: /
        pathType: Prefix
