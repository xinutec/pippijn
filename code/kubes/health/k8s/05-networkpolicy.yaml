# GENERATED from dhall/apps/health.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: "health-db-from-namespace"
  namespace: health
spec:
  ingress:
  - from:
    - podSelector: {}
    ports:
    - port: 3306
      protocol: TCP
  podSelector:
    matchLabels:
      app: "health-db"
  policyTypes:
  - Ingress

