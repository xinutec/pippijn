# GENERATED from dhall/apps/utterance.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.

---
# dev-lint: allow-backup-coverage utterance is under heavy development and its uploads are re-derivable; backing it up is deliberately deferred
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "utterance-data-pvc"
  namespace: utterance
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
