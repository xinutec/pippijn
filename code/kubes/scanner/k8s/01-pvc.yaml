# GENERATED from dhall/apps/scanner.dhall by dhall/generate.sh — do not edit.
# Change the model and re-render; hand edits are overwritten.

---
# dev-lint: allow-backup-coverage scanner's sessions are recordings of scans the Mac still holds; they are input for replay scoring rather than a primary copy
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "scanner-data-pvc"
  namespace: scanner
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
